# changelog.d — the release queue

Every PR adds ONE fragment here instead of editing `CHANGELOG.md`: `<slug>.<type>.md`, type ∈ added | changed | fixed |
removed | security. Body = the bullet(s), no header. The release cut folds them into a dated section.
